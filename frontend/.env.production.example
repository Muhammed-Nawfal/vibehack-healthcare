# If the frontend and backend are served from different domains in production,
# set this at build time to the backend API prefix.
#
# If they share one domain behind a reverse proxy, you can omit this and the app
# will call same-origin /api by default.
VITE_API_BASE_URL=https://your-backend-domain.com/api
